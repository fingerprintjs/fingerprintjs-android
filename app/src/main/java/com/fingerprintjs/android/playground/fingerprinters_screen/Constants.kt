package com.fingerprintjs.android.playground.fingerprinters_screen


// need to provide latest version in a single place
// also need to change README.md so that we don't always have to sync latest version throught
// all project
const val DEFAULT_FINGERPRINTER_VERSION = 4